{
  "name": "heroku-node-telegram-bot",
  "version": "1.0.0",
  "description": "Starter pack for running telegram bot on the Heroku using Node.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "SET >> .env && node index.js",
    "set_env": "SET >> .env",
    "switch_to_dev": "SET >> .env && start https://api.telegram.org/bot$TOKEN/setWebhook?url= && node index.js"
  },
  "author": "Volodymyr Lut",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "node-telegram-bot-api": "^0.21.1"
  }
}
